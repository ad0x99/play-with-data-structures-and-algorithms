/**
 * The Node class represents a node in a linked list with a value and a reference to the next node.
 */
export class Node {
  constructor(value) {
    this.value = value;
    this.next = null;
  }
}
